// Objetos

// Antes de usar objetos
let namePerson = 'César';
let career = 'React Developer';

console.log(namePerson);
console.log(career);

// Usando objetos
const person = {
  name: 'César',
  career: 'React Developer'
}

console.log(person);
console.log(person.name);
console.log(person['name']);  //otra forma de acceder a un valor dentro de un objeto

// ------------------------------------------------------------------------------------ //

// Object Constructor
// Permite crear objetos con distintos valores, es la manera antigua de realizar esta tarea
